name: Testing & Linting

on: [push]

jobs:
  Cool-Test:
    runs-on: ubuntu-latest
    steps:
     - name: Installing dependencies
       run: npm ci

     - name: Running tests
       run: npm run test
